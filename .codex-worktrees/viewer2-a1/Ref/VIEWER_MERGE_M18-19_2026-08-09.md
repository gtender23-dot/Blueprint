# Codex viewer merge — M18 + M19 (2026-08-09)

## Chain
M18 base `0d7a4975bd` (our M17-merge handoff) → M18 `3d27b24143` → M19
`45e41d80ad`. Merged onto our mainline `8e1a45b556` (M17 chain + Pass 7 +
pressure pie). Both git patches applied CLEAN. Merged build: `8148deb3f0`.

## Content
- **M18 — full-unit special teams + possession integrity.** Complete 11-v-11
  kick/punt/FG units with 15 new ST motion states. **FIRST Codex ENGINE
  TOUCH** (`js/engine/sim.js`): a real bug fix — in live every-snap games a
  post-score kickoff lived only in the current sim call, so pausing at the
  receiving team's call sheet discarded it (live sequence could show the
  scoring team retaining offense). Kickoffs now attach to the receiving drive
  before the live pause, with explicit `kickingSide`/`receivingSide` metadata
  (covers onside, squib, and the double-kick after a KO-return TD).
- **M19 — grounded locomotion.** Velocity-hysteresis controller around the
  jointed body: start/walk/jog/sprint/brake/planted-redirect states, facing
  hysteresis, backpedal/shuffle recognition, ground-phase-locked bob/cadence.
  Viewer-only (sprite.js + style.css).

## Merge verification
- sprite.js / style.css / app.js / sound.js byte-identical to M19 finals.
- sim.js = M18 finished + exactly the pressure-pie blocks (diff audited —
  every differing line is the pie).
- Codex ran OUR gate on their end (M19 kit ships tests/CORE-GATE.json: PASS,
  16 direct + 1 retry-cleared, 9.1 min local) and edited the manifest under
  its own policy (cutday_recs marked seedFlaky with a note) — the shared-
  infrastructure loop is working. Their manifest edit merged beside our
  blitz_pie entry without conflict.
- Our gate core on the merged tree: **PASSED — 17 OK + 1 flaky-cleared,
  12.0 min** (includes stat_realism 500 + pass7_band_ab 300 + blitz_pie —
  the M18 engine change is band-clean).

## Probes installed (no name collisions this time)
special_teams_animation_contract (fast source contract, 20/20) ·
live_score_kickoff_probe (possession fix, 0/262 wrong) · locomotion_probe
(deterministic controller, 15/15) · locomotion_live_probe (pw, green).
All registered in the manifest (full tier).

## Env note
M15-era `special_teams_live_probe` now flakes in the CLOUD container on the
"all four kick phases in one play" check: M18 narrowed the phase windows
(contact = ±0.13 of normalized play time) and the throttled headless rAF can
jump any one window per run. Each phase was verified individually in cloud
across runs, markup is byte-identical to Codex finals, and the full check
passed on a local machine (their M18 validation). Marked envKnown+seedFlaky
in the manifest with this note — if it reds LOCALLY, that's real.

## Handoff
Codex's next milestone must be cut against `8148deb3f0` or later (this build
= their M19 + the pressure pie; their M19-playable does NOT contain the pie).
